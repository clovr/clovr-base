[mothur_trim_seqs default]
$;INPUT_FILE_LIST$; = ${INPUT_FILE_LIST}
$;OLIGOS_FILE$; = ${OLIGOS_FILE}

[mothur_align_seqs default]
$;TEMPLATE_FILE$; = ${ALIGN_DB}

[mothur_classify_seqs default]
$;TEMPLATE$; = ${CLASSIFY_DB}
$;TAXONOMY$; = ${TAXONOMY_DB}

[qiime_mothur_cnvr default]
$;INPUT_FILE_LIST$;=${FASTA_FILES}
$;MAPPING_FILE$; = ${MAPPING_FILE}
$;MIN_LENGTH$; = 100
$;BARCODES$;=variable_length

[core_qiime default]
$;CUSTOM_PARAMETERS_FILE$; = /opt/bioinf/qiime/db/custom_params.txt

[metastats default]
$;GROUP_COUNT$; = ${GROUP_COUNT}

[metastats qiime]
$;GROUP_COUNT$; = ${GROUP_COUNT}

[skiff default]
$;GROUP_COUNT$; = ${GROUP_COUNT}

[skiff qiime]
$;GROUP_COUNT$; = ${GROUP_COUNT}

[mothur_align_seqs default]
$;TEMPLATE_FILE$; = ${DB_PATH}/core_set_aligned.imputed.fasta

[mothur_collect_single default]
$;GROUP_COUNT$; = ${GROUP_COUNT}

[mothur_rarefaction_single default]
$;GROUP_COUNT$; = ${GROUP_COUNT}

[mothur_summary single]
$;GROUP_COUNT$; = ${GROUP_COUNT}

[tag_data default]
$;INPUT_FILE$; = ${DOWNLOAD_MAP}



[prepare_paired_ends_for_velvet default]
$;SHORT_INPUT_LIST$; = ${SHORT_PAIRED_LIST}
$;LONG_INPUT_LIST$; = ${LONG_PAIRED_LIST}

[velvet_optimiser assembly]
$;SHORT$; = ${SHORT_INPUT_LIST}
$;LONG$; = ${LONG_INPUT_LIST}
$;START_HASH_LENGTH$; = ${START_HASH_LENGTH}
$;END_HASH_LENGTH$; = ${END_HASH_LENGTH}
$;OTHER_OPTIMISER_OPTS$; = -a yes -o "${VELVETG_OPTS} -min_contig_lgth 500"

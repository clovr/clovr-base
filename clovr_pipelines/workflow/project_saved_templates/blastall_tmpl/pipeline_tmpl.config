[split_multifasta default]
$;INPUT_FILE_LIST$; = ${INPUT_FILE_LIST}
$;SEQS_PER_FILE$; = ${SEQS_PER_FILE}

[ncbi-blastall default]
$;DATABASE_PATH$;=${REF_DB_PATH}
$;OTHER_OPTS$;=${OTHER_OPTS}
$;EXPECT$;=${EXPECT}

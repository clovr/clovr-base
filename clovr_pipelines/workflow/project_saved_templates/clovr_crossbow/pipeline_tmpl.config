[crossbow default]
$;INPUT_FILE_LIST$; = ${input.REFERENCE_TAG}
$;OUTPUT_PREFIX$; = ${params.OUTPUT_PREFIX}
$;REFERENCE_JAR$; = ${input.REFERENCE_SEQUENCES}

[tag_data clovr_crossbow]
$;PIPELINE_NAME$; = ${input.PIPELINE_NAME}

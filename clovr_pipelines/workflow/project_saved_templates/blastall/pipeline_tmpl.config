[split_multifasta default]
$;INPUT_FILE_LIST$; = ${INPUT_TAG}
$;SEQS_PER_FILE$; = ${SEQS_PER_FILE}

[ncbi-blastall default]
$;DATABASE_PATH$;=${REF_DB_TAG}
$;OTHER_OPTS$;=-p ${PROGRAM} ${OTHER_OPTS}
$;EXPECT$;=${EXPECT}
